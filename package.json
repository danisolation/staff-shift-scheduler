{
  "name": "staff-shift-scheduler",
  "version": "0.1.0",
  "private": true,
  "description": "Staff shift scheduler: mixed-integer optimization assigns employees to shifts",
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "test:e2e": "playwright test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "prettier": "^3.6.0",
    "turbo": "^2.5.0"
  }
}
