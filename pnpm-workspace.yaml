packages:
  - "apps/*"
  - "packages/*"

allowBuilds:
  # esbuild's postinstall downloads its native binary — required.
  esbuild: true
  # @scarf/scarf is install telemetry, not functionality — denied.
  '@scarf/scarf': false
